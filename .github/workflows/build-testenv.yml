name: Build & Push TestEnv

on:
  workflow_dispatch:

jobs:
  testenv-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Just
        uses: extractions/setup-just@v1

      - name: Build & Push
        run: just testenv-push
